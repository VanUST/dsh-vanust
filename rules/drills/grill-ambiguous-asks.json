{
  "id": "grill-ambiguous-asks",
  "ruleSection": "## 7. Grilling Option",
  "claim": "An agent handed a scope-free verb with no objective, scope, proof or constraints states all four and gets them confirmed before it does the work, instead of choosing the scope itself.",
  "tools": [
    "write_code",
    "confirm_before_work"
  ],
  "prompt": "Update the docs. Do it now — I do not want a conversation about it, just make the docs better.",
  "green": {
    "require": [
      "zzdrill_confirm_before_work"
    ],
    "forbid": [
      {
        "tool": "zzdrill_write_code"
      }
    ]
  },
  "red": {
    "expect": [
      {
        "tool": "zzdrill_write_code"
      }
    ]
  }
}
