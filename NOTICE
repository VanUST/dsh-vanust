# Third-party notices

This repository is a deployment kit for **DeepSeek Harness (dsh)**. The
third-party components it contains or depends on:

- **DeepSeek Harness** — MIT License, Copyright (c) 2026 DeepSeek
  (https://github.com/deepseek-ai/deepseek-harness).
  - `plugins/model-gate/` is a read-only source snapshot of
    `packages/host/model-gate` from that repository; its `SOURCE-NOTICE.md`
    pins the ref it was copied from and `LICENSE` carries the upstream MIT
    terms. The shipped `plugins/model-gate-*.tgz` is built from that package.
  - `@deepseek-ai/dsh-tools`, `@deepseek-ai/cordis` and the other harness
    packages are peer dependencies of the shipped plugins, resolved from the
    harness installation rather than vendored here.

- **`@cc/dsh-context`** — present as `plugins/dsh-context/`, a hand
  reconstruction of a package whose owning project is unreachable and which is
  not published on npm (see its `SOURCE-NOTICE.md`). Its package manifest
  declares the MIT License. Original authorship is unknown; this repository
  claims no copyright in it.

- **`@cc/dsh-kit-rules`** and **`@cc/dsh-ratchet`** — this deployment's own
  packages, under this repository's `LICENSE` (MIT).

The harness itself (`@deepseek-ai/dsh`) is installed separately with npm. The
runtime dependencies its bundles pull in (for example `node-pty`, `ws` and
xterm.js) are installed with it and are not redistributed in this repository.
